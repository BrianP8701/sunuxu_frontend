import React from 'react';
import { useSelector } from 'react-redux';

type PersonViewProps = {
    id: string;
};

const PersonView: React.FC<PersonViewProps> = ({ id }) => {



    return (
        <div>

        </div>
    );
};

export default PersonView;
